<UserControl x:Class="RegulationAssistantChatClient.Views.OptionsView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 xmlns:conv="clr-namespace:RegulationAssistantChatClient.Converters"
 mc:Ignorable="d"
 d:DesignHeight="300" d:DesignWidth="400">

  <UserControl.Resources>
	<conv:LanguageToResourceConverter x:Key="LanguageToResourceConverter" />
  </UserControl.Resources>

  <Grid Margin="10">
	<Grid.RowDefinitions>
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="*" />
	</Grid.RowDefinitions>
	<Grid.ColumnDefinitions>
	  <ColumnDefinition Width="Auto" />
	  <ColumnDefinition Width="*" />
	</Grid.ColumnDefinitions>

	<TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding Language, Converter={StaticResource LanguageToResourceConverter}, ConverterParameter=OptionsHeader}" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

	<TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding Language, Converter={StaticResource LanguageToResourceConverter}, ConverterParameter=LanguageLabel}" Margin="0,0,10,0" VerticalAlignment="Center" />
	<ComboBox Grid.Row="1" Grid.Column="1" SelectedValue="{Binding Language, Mode=TwoWay}" SelectedValuePath="Tag" HorizontalAlignment="Left" VerticalAlignment="Center">
	  <ComboBoxItem Tag="English" Content="{Binding Language, Converter={StaticResource LanguageToResourceConverter}, ConverterParameter=LanguageOption_English}" />
	  <ComboBoxItem Tag="Serbian" Content="{Binding Language, Converter={StaticResource LanguageToResourceConverter}, ConverterParameter=LanguageOption_Serbian}" />
	</ComboBox>

	<TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding Language, Converter={StaticResource LanguageToResourceConverter}, ConverterParameter=AnswerStyleLabel}" Margin="0,0,10,0" VerticalAlignment="Center" />
	<ComboBox Grid.Row="2" Grid.Column="1" SelectedValue="{Binding ResponseType, Mode=TwoWay}" SelectedValuePath="Tag" HorizontalAlignment="Left" VerticalAlignment="Center">
	  <ComboBoxItem Tag="Concise with Citations" Content="{Binding Language, Converter={StaticResource LanguageToResourceConverter}, ConverterParameter=AnswerStyle_Concise}" />
	  <ComboBoxItem Tag="Detailed" Content="{Binding Language, Converter={StaticResource LanguageToResourceConverter}, ConverterParameter=AnswerStyle_Detailed}" />
	  <ComboBoxItem Tag="Bullet Points" Content="{Binding Language, Converter={StaticResource LanguageToResourceConverter}, ConverterParameter=AnswerStyle_BulletPoints}" />
	</ComboBox>
  </Grid>
</UserControl>