<UserControl x:Class="RegulationAssistantChatClient.Views.DocumentEditView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 mc:Ignorable="d"
 d:DesignHeight="200" d:DesignWidth="400">
  <Grid Margin="10">
	<Grid.RowDefinitions>
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="Auto" />
	</Grid.RowDefinitions>

	<TextBlock Text="Title:" />
	<TextBox Grid.Row="1" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,10" />

	<TextBlock Grid.Row="2" Text="Version:" />
	<TextBox Grid.Row="3" Text="{Binding VersionNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,10" />

	<StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
	  <Button x:Name="OkButton" Content="OK" Width="80" Margin="0,0,10,0" />
	  <Button x:Name="CancelButton" Content="Cancel" Width="80" />
	</StackPanel>
  </Grid>
</UserControl>